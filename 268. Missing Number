class Solution {
    public int missingNumber(int[] nums) {
        int a_sum = 0, sum=0, n=nums.length;
        for(int ele : nums) a_sum+=ele;
        sum = (n*(n+1))/2;
        return sum-a_sum;
    }
}
